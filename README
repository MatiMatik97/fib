Mateusz Kozak - Grupa 2.2.3
Programowanie full-stack w chmurze obliczeniowej
Laboratorium 8

W zadaniu został wykorzystany prosty serwer HTML - httpd.

--

Polecenia wykorzystane do realizacji zadania:

GitHub:
<< git clone https://github.com/MatiMatik97/fib >> - Pobranie wcześniej utworzonego repozytorium na dysk
<< cd fib >> - przejście do folderu
<< git pull >> - upewnienie się, że mamy najnowszą wersję repozytorium
<< git config --global user.name "nazwa uzytkownika" >> - ustawienie nazwy użytkownika
<< git config --global user.email "adres email" >> - ustawienie adresu e-mail
<< git remote set-url origin https://MatiMatik97:TOKEN@github.com/MatiMatik97/fib.git >> - ustawienie tokenu dostępu
<< git add . >> - dodanie zmienionych plików
<< git commit -m "opis zmian" >> - zatwierdzenie wprowadzonych zmian
<< git push origin master >> - wysłanie zmian do repozytorium

Docker:
<< docker build -t nazwa_obrazu . >> - zbudowanie obrazu
<< docker run --name nazwa_kontenera -p 8080:80 nazwa_obrazu >> - uruchomienie kontenera na podanym porcie